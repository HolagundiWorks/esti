{
  "name": "@esti/e2e",
  "private": true,
  "type": "module",
  "description": "AORMS browser automation suite (Playwright) — logins, navigation sweep, button crawler, PDF generation.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:auth": "playwright test auth",
    "test:nav": "playwright test navigation",
    "test:buttons": "playwright test buttons",
    "test:crud": "playwright test crud",
    "test:a11y": "playwright test accessibility",
    "report": "playwright show-report",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "1.49.0"
  }
}
