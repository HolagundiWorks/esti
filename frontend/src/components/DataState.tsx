/**
 * DataState — promoted into the design system (2026-07). This file remains as a
 * compatibility re-export so the 40+ existing call sites keep their import path;
 * new code should import from `@hcw/ui-kit` directly.
 */
export { DataState } from "@hcw/ui-kit";
